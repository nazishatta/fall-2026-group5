# MNIST v1 Markdown Reports (`reports/Markdown_Report/v1_mnist/`)

This directory contains standalone Markdown reports documenting experiment milestones and results for the MNIST v1 dataset:

- `cnn_baseline_results.md`: Full summary of LeNet-5 baseline performance, test set error breakdown, and embedding properties.
