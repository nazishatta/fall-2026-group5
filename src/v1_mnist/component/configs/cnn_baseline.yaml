# ==============================================================================
# Milestone Configuration: Baseline Data Preparation & LeNet-5 Training
# Outputs saved to: ./outputs/v1_mnist/cnn_baseline/
# ==============================================================================

run:
  name: cnn_baseline
  version: v1_mnist
  stage: baseline

tasks:
  prepare_data: true
  train_baseline: true
  extract_embeddings: true
